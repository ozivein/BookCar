@model List<ResultCategoryDto>
<div class="sidebar-box ftco-animate">
    <div class="categories">
        <h3>Kategoriler</h3>
        @if (Model.Count > 0)
        {
            foreach (var item in Model)
            {
                <li><a href="#">@item.Name <span>(0)</span></a></li>
            }

        }
        else
        {
            <li><a href="#">Kategori Yok <span>(0)</span></a></li>
        }
    </div>
</div>